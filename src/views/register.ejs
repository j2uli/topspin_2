<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Registro de Usuario - TopSpin</title>
  <link rel="stylesheet" href="/css/register.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
  <div class="container">
    <div class="logo-section">
      <img src="/img/logo.png" alt="TopSpin Logo" class="logo flipped" />
    </div>
    <div class="form-section">
      <h2>Regístrate</h2>
      <form id="registerForm" action="/register" method="POST">
        <input type="text" name="nombre" placeholder="Nombre completo" required />
        <input type="email" name="correo" placeholder="Correo electrónico" required />
        <input type="text" name="telefono" placeholder="Teléfono" required />
         <div class="password-wrapper">
          <input type="password" name="contraseña" id="contraseña" placeholder="Contraseña" required />
          <span class="toggle-password" id="togglePassword">
           <i class="fas fa-eye"></i>
          </span>
        </div>


        <div id="passwordRequirements" class="password-requirements" style="display: none;">
          <p><span id="uppercaseRequirement" class="invalid">Debe contener al menos una letra mayúscula</span></p>
          <p><span id="lowercaseRequirement" class="invalid">Debe contener al menos una letra minúscula</span></p>
          <p><span id="numberRequirement" class="invalid">Debe contener al menos un número</span></p>
          <p><span id="specialCharRequirement" class="invalid">Debe contener al menos un carácter especial</span></p>
        </div>

       
        <input type="text" name="direccion" placeholder="Dirección" required />
        
        <select name="rol" required>
          <option value="">Selecciona tu rol</option>
          <option value="cliente">Cliente</option>
          <option value="tecnico">Técnico</option>
          <option value="admin">Administrador</option>
        </select>

        <input type="date" name="fecha_nacimiento" required max="<%= maxDate %>" />

        <button type="submit">Registrarse</button>
      </form>

      <p class="login-prompt">
        ¿Ya tienes cuenta?
        <a href="/login" class="login-link">Iniciar sesión</a>
      </p>
    </div>
  </div>


 <% if (notificacion) { %>
  <div class="notification <%= notificacion.tipo %> show">
    <strong><%= notificacion.tipo === 'success' ? '¡Éxito!' : 'Error' %>:</strong> <%= notificacion.mensaje %>
  </div>
<% } %>

  <script src="/js/register.js"></script>
</body>
</html>
